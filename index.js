const express = require('express');
const app = express();
const port = 3000;

app.get();
app.post();
app.put();
app.delete();

app.listen(port, () => {
  console.log(`Listening on port ${port}...`);
});